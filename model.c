//cria um conjunto de variaveis(criando a váriavel peça)
 typedef struct _peca{
    int le;
    int ld;
} peca;

//declaração das variáveis globais
peca pecasDomino[28];
peca pecasjogo[28];
int a = 0,b = 0, c = 0;
